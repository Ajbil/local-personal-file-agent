# Credential Policy

Employees must rotate their credentials every ninety days. A replacement credential must not reuse
any of the previous twelve values. Accounts with suspected exposure are reset immediately rather
than waiting for the normal rotation interval.
